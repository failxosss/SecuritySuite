# Copy this file to plugins/SecuritySuite/secrets.yml and fill in real values.
# secrets.yml is loaded in addition to config.yml; any key set here overrides
# the matching key in config.yml. This file is never read into log output
# and SecuritySuite will refuse to print its contents even in debug mode.
#
# secrets.yml is created automatically (empty) on first run if missing.

providers:
  ipqualityscore:
    api-key: ""
  custom:
    api-key: ""

discord:
  webhook-url: ""

database:
  mysql:
    password: ""
